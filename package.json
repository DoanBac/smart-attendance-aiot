{
  "name": "smart-attendance-aiot",
  "version": "1.0.0",
  "private": true,
  "description": "AIoT Smart Attendance System — monorepo root",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "lint": "cd frontend && npm run lint"
  }
}
