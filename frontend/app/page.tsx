/** @type {import('next').NextConfig} */
const nextConfig = {}
module.exports = nextConfig

// This file is intentionally left blank.
// The main app is in src/app/
export default function Page() {
  return null;
}